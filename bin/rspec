#!/usr/bin/env ruby
# frozen_string_literal: true

load File.expand_path("../with_rubygems", __FILE__) if ENV["RGV"]

require_relative "../spec/support/rubygems_ext"

Spec::Rubygems.safe_load("rspec", "~> 3.6", "rspec-core")
